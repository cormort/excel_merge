# 基金資料篩選器 - 專案說明

## 專案目標

簡化現有 Excel 基金資料篩選工具的介面與操作流程，直接產出最終需要的基金資料。

## 使用流程

1. **匯入檔案** - 拖放或選取多個 Excel 檔案（.xls/.xlsx/.ods）
2. **自動識別** - 系統自動根據 fund-config.json（282 個標準基金）識別每個檔案屬於哪個基金
3. **基金對照** - 顯示每個檔案對應的標準基金，可勾選要彙整的檔案
4. **分析資料** - 點擊分析後掃描資料列，可選擇比對第一、第二或第三欄與標準基金名稱
5. **標記可疑列** - 將選取欄位與標準基金名稱不匹配的列預設勾選，供刪除候選
6. **人工審核** - 使用者檢查並刪除不要的資料列
7. **匯出結果** - 匯出保留的資料（包含標準基金、繳交狀態、來源檔案欄位）

## 核心功能

### 檔案匯入與辨識
- 支援 .xls、.xlsx、.ods 格式
- 可拖放或點擊選取多個檔案
- 支援設定工作表匯入方式（僅第一張、所有工作表、指定位置、指定名稱）
- 自動移除空白列、剔除含關鍵字（小計、總計、合計）的列

### 自動基金識別
- 讀取 fund-config.json 獲取標準基金清單（名稱 + 別名）
- 掃描每個檔案的資料列，識別屬於哪個基金
- 支援完全匹配或前4字+字數相同的模糊匹配

### 基金對照總覽
- 顯示每個檔案對應的標準基金
- 提供全選、全不選、反選功能
- 勾選後可執行分析

### 彙整資料預覽
- 固定顯示第一、第二、第三欄
- 可透過下拉選單選擇要與標準基金比對的欄位
- 選取的比對欄位會藍色高亮顯示
- 自動標記「選取欄位與標準基金不符」的列為可疑列
- 可勾選刪除不要的列

### 資料匯出
- 匯出所有未刪除的資料列
- 自動產生「標準基金」欄位（依 fund-config.json 排序）
- 自動產生「繳交狀態」欄位（✅已回傳 / ❌未回傳/無資料）
- 保留來源檔案與原始欄位結構

## 技術參考

- fund-config.json: 標準基金名稱與別名對照表（282 個基金）
- Fund Mapping 模組：
  - `getFundSearchKeys()` - 建立搜尋關鍵字
  - `detectFundsInText()` - 從文字中識別基金
  - `cellMatchesFund()` - 判斷儲存格是否符合標準基金

## 版本紀錄

### v1.2（2026/04）
- 新增比對欄位選擇功能（第一、第二、第三欄）
- 彙整資料預覽固定顯示前三欄
- 選取的比對欄位藍色高亮顯示

### v1.1（2024/04）
- 新增 ODS 檔案支援
- 改進基金比對邏輯（完全匹配或前4字+字數相同）
- 新增彙整資料第二欄顯示
- 新增分析後自動滾動到彙整區塊